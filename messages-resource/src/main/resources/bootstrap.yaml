server:
  port: 8090
spring:
  application:
    name: messages-resource
  cloud:
    nacos:
      discovery:
        server-addr: nacos-server:8848
      config:
        server-addr: nacos-server:8848